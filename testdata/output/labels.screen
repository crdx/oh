=== narrow ===

 run each shape of command and load the
 skill


$ if [[ -d src ]]; then echo there… ✓ 1L
$ while read -r NAME; do echo "$NA… ✓ 1L
$ until [[ -z "$REST" ]]; do REST=… ✓ 1L
$ case "$MODE" in read) echo r ;; … ✓ 1L
$ select CHOICE in one two; do bre… ✓ 1L
$ ls -la | grep -v "^d" | wc -l ✓ 1L
grep ^\\p{Lu}\\w{2,8}(?:-\\d+)?$ *.go ✓ 1L
load skills/tidy/SKILL.md ✓ 8L
read wide.md ✓ 3L
read screen.go 10-14 ✓ 5L
read screen.go 10+ ✓ 5L
read screen.go 1-5 ✓ 5L

The listing is 日本語のテキストと絵文字
🎉 が混ざった行, which is wider on
screen than it is long.

I left a note in ~/README.md and the
skill is at ~/skills/tidy/SKILL.md.

// count reports how many there were.
func count() int {
    var total int = 42
    return total
}
=== one column ===


r

u

n

e

a

c

h

s

h

a

p

e

o

f

c

o

m

m

a

n

d

a

n

d

l

o

a

d

t

h

e

s

k

i

l

l


✓
✓
✓
✓
✓
✓
✓
✓
✓
✓
✓
✓

T
h
e
l
i
s
t
i
n
g
i
s

日
本
語
の
テ
キ
ス
ト
と
絵
文
字

🎉

が
混
ざ
っ
た
行
,
w
h
i
c
h
i
s
w
i
d
e
r
o
n
s
c
r
e
e
n
t
h
a
n
i
t
i
s
l
o
n
g
.

I
l
e
f
t
a
n
o
t
e
i
n
~
/
R
E
A
D
M
E
.
m
d
a
n
d
t
h
e
s
k
i
l
l
i
s
a
t
~
/
s
k
i
l
l
s
/
t
i
d
y
/
S
K
I
L
L
.
m
d
.

/
/
c
o
u
n
t
r
e
p
o
r
t
s
h
o
w
m
a
n
y
t
h
e
r
e
w
e
r
e
.
f
u
n
c
c
o
u
n
t
(
)
i
n
t
{

v
a
r
t
o
t
a
l
i
n
t
=
4
2

r
e
t
u
r
n
t
o
t
a
l
}
=== printed ===

 run each shape of command and load the skill


$ if [[ -d src ]]; then echo there; elif [[ -f src ]]; then echo file; else echo none; fi ✓ 1L
$ while read -r NAME; do echo "$NAME" >out.txt 2>&1; done <list.txt ✓ 1L
$ until [[ -z "$REST" ]]; do REST=""; done ✓ 1L
$ case "$MODE" in read) echo r ;; write) echo w ;; *) echo o ;; esac ✓ 1L
$ select CHOICE in one two; do break; done ✓ 1L
$ ls -la | grep -v "^d" | wc -l ✓ 1L
grep ^\\p{Lu}\\w{2,8}(?:-\\d+)?$ *.go ✓ 1L
load skills/tidy/SKILL.md ✓ 8L
read wide.md ✓ 3L
read screen.go 10-14 ✓ 5L
read screen.go 10+ ✓ 5L
read screen.go 1-5 ✓ 5L

The listing is 日本語のテキストと絵文字 🎉 が混ざった行, which is wider on screen than it is long.

I left a note in ~/README.md and the skill is at ~/skills/tidy/SKILL.md.

// count reports how many there were.
func count() int {
    var total int = 42
    return total
}
=== tiny ===

 run each
 shape of
 command and
 load the
 skill


$ if [… ✓ 1L
$ whil… ✓ 1L
$ unti… ✓ 1L
$ case… ✓ 1L
$ sele… ✓ 1L
$ ls -… ✓ 1L
grep ^… ✓ 1L
load s… ✓ 8L
read w… ✓ 3L
read s… ✓ 5L
read s… ✓ 5L
read s… ✓ 5L

The listing
is
日本語のテキ
ストと絵文字
🎉
が混ざった行
, which is
wider on
screen than
it is long.

I left a
note in
~/README.md
and the
skill is at
~/skills/tid
y/SKILL.md.

// count
reports how
many there
were.
func count()
int {
    var
total int =
42
    return
total
}
=== wide ===

 run each shape of command and load the skill


$ if [[ -d src ]]; then echo there; elif [[ -f src ]]; then echo file; else echo none; fi ✓ 1L
$ while read -r NAME; do echo "$NAME" >out.txt 2>&1; done <list.txt ✓ 1L
$ until [[ -z "$REST" ]]; do REST=""; done ✓ 1L
$ case "$MODE" in read) echo r ;; write) echo w ;; *) echo o ;; esac ✓ 1L
$ select CHOICE in one two; do break; done ✓ 1L
$ ls -la | grep -v "^d" | wc -l ✓ 1L
grep ^\\p{Lu}\\w{2,8}(?:-\\d+)?$ *.go ✓ 1L
load skills/tidy/SKILL.md ✓ 8L
read wide.md ✓ 3L
read screen.go 10-14 ✓ 5L
read screen.go 10+ ✓ 5L
read screen.go 1-5 ✓ 5L

The listing is 日本語のテキストと絵文字 🎉 が混ざった行, which is wider on screen than it is long.

I left a note in ~/README.md and the skill is at ~/skills/tidy/SKILL.md.

// count reports how many there were.
func count() int {
    var total int = 42
    return total
}
